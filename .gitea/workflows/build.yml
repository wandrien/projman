name: Projman build the packages
run-name: ${{ gitea.actor }} build projman packages
on: [push]

jobs:
  Projman-Build:
    runs-on: 056c54762ffa:docker://svk28/tcltk-build-debian:latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - run: cd debian
      - run: ./build-deb-projman.sh
